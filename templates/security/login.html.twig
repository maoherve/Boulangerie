{% extends 'layout.html.twig' %}

{% block title %}Connexion !{% endblock %}

{% block main %}
<form method="post" class="container container-fluid mt-5">
    <!-- error message if login of password wrong  -->
    {% if error %}
        <div class="alert alert-danger mt-5">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <!-- if log tel the username  -->
    {% if app.user %}
        <div class="mb-3">
            Vous etes connecter en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}">Déconnexion</a>
        </div>
    {% endif %}

    <h2 class="titleLogin h3 mb-3 font-weight-normal">Connectez vous !</h2>
    <label for="inputUsername">Nom d'utilisateur</label>
    <input type="text" value="{{ last_username }}" name="username" id="inputUsername" class="form-control" required autofocus>
    <label for="inputPassword">Mot de passe</label>
    <input type="password" name="password" id="inputPassword" class="form-control" required>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}">

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    <button class="btn btn-lg btn-primary" type="submit">
        connexion
    </button>
</form>
{% endblock %}


{% block footer %}
    {% include 'includes/_fixedFooter.html.twig' %}
{% endblock %}