{% extends 'adminLayout.html.twig' %}


{% block main %}
    <div class="adminContainer container container-fluid">
        <h2>Modifier un produits :</h2>

            {{ include('admin/products/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            {{ include('admin/products/_delete_form.html.twig') }}

            <a href="{{ path('admin_products_index') }}">Retour à la liste.</a>
    </div>
{% endblock %}
