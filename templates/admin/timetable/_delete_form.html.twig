<form method="post" action="{{ path('admin_timetable_delete', {'id': timetable.id}) }}" onsubmit="return confirm('La suppression est irréversible, êtes-vous sûr de vouloir continuer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ timetable.id) }}">
    <button class="deleteBtn btn mt-2">Supprimer</button>
</form>
