<form method="post" action="{{ path('admin_homeTexte_delete', {'id': home_texte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home_texte.id) }}">
    <button class="btn">Delete</button>
</form>
