<!DOCTYPE html>

<title>Edit Timetable</title>

{% block body %}
    <h1>Edit Timetable</h1>

    {{ include('admin/timetable/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('timetable_index') }}">back to list</a>

    {{ include('admin/timetable/_delete_form.html.twig') }}
{% endblock %}
