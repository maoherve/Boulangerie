{% extends 'adminLayout.html.twig' %}


{% block main %}

    <div class="adminContainer container container-fluid">
        <h2>Menu des images du carousel :</h2>

            <table class="table">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Titre</th>
                        <th>Image</th>
                        <th>actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for carousel in carousels %}
                    <tr>
                        <td>{{ carousel.id }}</td>
                        <td>{{ carousel.title }}</td>
                        <td>{{ carousel.imageName }}</td>
                        <td>
                            <a href="{{ path('admin_carousel_edit', {'id': carousel.id}) }}">edit</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">Pas de carouseul disponible !</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            <a href="{{ path('admin_carousel_new') }}">Créer une nouvelle image du carousel !</a>
    </div>
{% endblock %}
